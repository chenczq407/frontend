schema: spec-driven

context: |
  Product platform: WeChat Mini Program
  Student-side frontend stack: uni-app + Vue 3 + TypeScript
  Vue conventions: Composition API with <script setup lang="ts"> by default
  Student-side styling stack: UnoCSS with unocss-preset-weapp, plus SCSS for page-specific details
  Scope: current OpenSpec changes define the student-side Mini Program only
  Non-goal for now: backend stack and admin/teacher web management decisions

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
